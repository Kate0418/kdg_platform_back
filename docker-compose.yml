services:
  app:
    build: ./
    ports:
      - "8081:8000"
    volumes:
      - .:/back
    restart: always
    command: php artisan serve --host 0.0.0.0
  worker:
    build: ./
    volumes:
      - .:/back
    restart: always
    command: php artisan queue:work
